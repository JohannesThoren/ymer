[workspace]

[package]
name = "script-host"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
rquickjs = { version = "0.13", features = ["bindgen"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
